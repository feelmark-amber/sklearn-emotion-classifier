# メインのPythonソースコードを格納するフォルダ (プロジェクト名でも可)
プロジェクトの心臓部。再利用可能なコード（関数やクラス）を機能ごとにサブフォルダに分けて整理します。
__init__.py を各フォルダに置くことで、Pythonがそれらを「パッケージ」として認識し、他のファイルから import src.training.train のように読み込めるようになります。
機能を分割することで、コードの見通しが良くなり、修正やテストがしやすくなります。